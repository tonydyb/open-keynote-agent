# Tasks: Add Image Assets To Storybook Renderer

## 1. Manifest Loading

- [x] Add image manifest loading helper for renderer/CLI use.
- [x] Parse with existing `ImageManifest` schema.
- [x] Resolve asset paths relative to manifest directory.
- [x] Reject absolute manifest asset paths.
- [x] Reject duplicate `slide_index` entries.
- [x] Reject missing listed asset files before Keynote mutation.
- [x] Return `dict[int, Path]` mapping slide index to absolute image path.
- [x] Add unit tests for valid manifest loading.
- [x] Add unit tests for duplicate indexes, absolute paths, and missing files.

## 2. AppleScript Builder

- [x] Add `scripts.add_image(slide, path, x, y, width, height)`.
- [x] Use existing AppleScript string escaping for path.
- [x] Convert image path to POSIX file in AppleScript.
- [x] Create image object on target slide.
- [x] Set position, width, and height with separate statements.
- [x] Keep `object_id` local-only; do not set Keynote object names for image items.
- [x] Return created image object collection index.
- [x] Add builder unit tests for escaping and geometry formatting.

## 3. Keynote Tool

- [x] Add `keynote.add_image` handler.
- [x] Validate `slide >= 1`.
- [x] Validate image path exists and is a file.
- [x] Convert image path to absolute path before calling script builder.
- [x] Validate geometry with existing object geometry validator.
- [x] Validate/generate object IDs using existing object helpers.
- [x] Register image object metadata with `type="image"`.
- [x] Store `path`, geometry, `apple_class`, and `apple_index` in object context.
- [x] Add object ID to `context["keynote"]["slides"][str(slide)]["objects"]`.
- [x] Register the tool in `register_keynote_tools`.
- [x] Add unit tests for successful add_image.
- [x] Add unit tests for missing file, invalid geometry, invalid object ID, and context updates.

## 4. Renderer Integration

- [x] Extend `render_storybook_deck(...)` to accept optional image asset mapping or manifest path.
- [x] Insert matching slide images with `keynote.add_image`.
- [x] Append image insertion results to `RenderResult.tool_results`.
- [x] Use image as primary visual when available.
- [x] Use full-bleed image placement when available.
- [x] Use blank layout for image-backed slides 2..N.
- [x] Skip default Keynote titles for image-backed slides 2..N.
- [x] Add deterministic overlay text after image insertion.
- [x] Avoid rendering large emoji primary visual when image is available.
- [x] Preserve emoji/shape fallback when image is missing.
- [x] Ignore extra image assets for slide indexes not in DeckSpec.
- [x] Preserve existing no-image renderer behavior.
- [x] Add unit tests for image insertion calls.
- [x] Add unit tests for fallback on missing slide assets.
- [x] Add unit tests that extra image assets do not create extra slides.
- [x] Add unit tests that failed add_image stops rendering clearly.

## 5. Layout Templates

- [x] Add deterministic image frame helpers to `renderers/templates.py`.
- [x] Define full-bleed image placement for image-backed slides.
- [x] Keep overlay text deterministic; automatic color/panel handling is deferred.
- [x] Keep all coordinates in the existing 1280x720 coordinate system.
- [x] Add tests for deterministic image object IDs and geometry.

## 6. CLI

- [x] Add `oka render-storybook --images PATH`.
- [x] Validate manifest and files before registering/mutating Keynote.
- [x] Pass loaded image assets to renderer.
- [x] Preserve `--output` and `--no-pdf`.
- [x] Print the image manifest path when used.
- [x] Write `render_result.json`.
- [x] Write `tool_results.jsonl` with image tool results.
- [x] Add CLI tests for `--images`.
- [x] Add CLI tests that invalid manifest exits before renderer mutation.
- [x] Add CLI tests that no-image behavior is unchanged.

## 7. Documentation

- [x] Update README with `render-storybook --images` workflow.
- [x] Update AGENTS.md and CLAUDE.md with image asset renderer architecture.
- [x] Document that 012 consumes images generated by 010/011 and does not generate images.
- [x] Document fallback behavior for missing slide images.
- [x] Add/update Chinese spec-reading notes.

## 8. Quality Bar

- [x] Run `uv run pytest tests/test_keynote_tools.py tests/test_storybook_renderer.py tests/test_images.py -q`.
- [x] Run `uv run pytest -q`.
- [x] Run `uv run ruff check .`.
- [ ] Optionally run `RUN_KEYNOTE_INTEGRATION=1 uv run pytest -m keynote_integration -s` on macOS with Keynote.
